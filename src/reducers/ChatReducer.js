const initialState = {
    chats: [],
    activeChat : '',
    isLoading: false,
    notifications : [],
}

const chatReducer = (state = initialState, action) => {
    switch (action.type) {
        
    }
}

export default chatReducer